<view class="list-content item_class" tt:for="{{listArray}}" tt:key="index"
    tt:if="{{listArray.length>0 && !item.hideStatus}}">
    <view class="list-title left_class">{{item.title}}</view>
    <view class="list-value right_class">{{item.value}}</view>
    <slot />
</view>